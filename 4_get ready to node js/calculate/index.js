const { multiplyNum } = require('./multiplyNum');
const { calculateSum } = require('../calculateSum')


// using this app.js will don't need to know that
module.exports = {
    multiplyNum,
    calculateSum
}